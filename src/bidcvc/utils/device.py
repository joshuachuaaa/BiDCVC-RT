"""Device utilities (stub).

Avoid importing torch at module import time.
"""

from __future__ import annotations


def default_device() -> str:
    """Return a string name of the default compute device (placeholder)."""

    return "cpu"

